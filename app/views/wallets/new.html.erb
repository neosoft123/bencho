<h1>New wallet</h1>

<% form_for(@wallet) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :balance %><br />
    <%= f.text_field :balance %>
  </p>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', wallets_path %>
